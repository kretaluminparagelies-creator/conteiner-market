# Container Market GR

> We sell and rent our own shipping containers in Greece — direct from Logiworkpass P.C.
> Not a third-party marketplace. Bilingual: Greek (default) + English (client toggle, localStorage key `cm-locale`).

## Site

- Production URL: https://containermarket.gr
- Vercel URL: https://conteiner-market.vercel.app
- Stack: Next.js 16, React 19, Supabase (listings + CRM leads)
- Version: 1.6.15
- AI guide (this file): /llms.txt
- Short pointer: /ai.txt

## Main routes

| Path | Description |
|------|-------------|
| / | Home — hero search, featured listings carousel, category cards, stats |
| /agora | Buy containers (service page; home category card opens carousel tab `offers`) |
| /polisi | Sell your container to us (buyback) |
| /enoikiasi | Rent our containers (service page; home category card opens carousel tab `rent`) |
| /enoikiasis-xoron | Storage space rental |
| /listings | Full catalog — tabs, filters, carousel (desktop) / pager (mobile) |
| /listings/{slug} | Individual listing — Product JSON-LD, photos, price, bilingual |
| /epikoinonia | Contact form → CRM leads, email, phone, coverage |
| /help | FAQ (JSON-LD FAQPage) — how to search, buy, rent, contact |
| /oroi | Terms of use (EL + EN) |
| /aporrito | Privacy policy / GDPR (EL + EN) |
| /cookies | Cookie policy (EL + EN) |
| /epistrofes | Returns & withdrawal policy (EL + EN) |

Redirects:
- /prosfores → /listings

Blocked (robots):
- /admin/* — CRM admin (login required, Supabase Auth)

## Language

- Default UI: Greek (el)
- English: toggle in nav (ΕΛ / EN) on desktop; mobile menu footer on small screens
- Legal, service pages, FAQ, listings descriptions support both locales
- Server HTML metadata defaults to Greek; title/description update client-side when EN is selected

## Home featured listings (carousel)

Section id: `prosfores` (home page, below hero).

Tabs (query param `tab`):
- `offers` — special offers (default)
- `new` — new stock
- `used` — used stock
- `rent` — rentals

Optional filters (combine with `tab`):
- `deal` — `sale` or `rent` (transaction type from hero search)
- `containerType` — comma-separated ISO catalog ids, e.g. `20dc`, `40hc`, `40dc`, `20-reefer`, `open-top`, `flat-rack`, `double-door`, `side-door`, `40-reefer`, `45hc`, `45-pallet-wide`

Examples:
- /?tab=offers
- /?tab=rent
- /?deal=sale&containerType=20dc,40hc
- /?tab=rent&deal=rent

Home category cards (ΑΓΟΡΑ / ΕΝΟΙΚΙΑΣΗ) scroll to this section and switch tab (`offers` / `rent`).

Mobile: one listing card with ‹ › arrows or swipe; tap card for details.
Desktop: 3D carousel; click center card for details.

Do NOT use `/listings?q=` — there is no free-text search parameter.

## Catalog page (/listings)

Uses carousel tabs plus optional filters:
- `tab` — same values as home (`offers`, `new`, `used`, `rent`)
- `deal` — `sale` or `rent`
- `size` — leading size token from listing type, e.g. `20ft`, `40ft`, `45ft`

Examples:
- /listings?deal=sale
- /listings?deal=rent&size=40ft
- /listings?tab=offers

## Listings data

- Primary source: Supabase `listings` table (fallback: static JSON)
- Active listings only on public site (`active = true`)
- Fields: id, slug, type, condition, price, priceFormatted, unit, location, listingType (sale|rent), stockCondition (new|used), isOffer, images, description
- Bilingual fields: conditionEn, locationEn, descriptionEn, unitEn
- Detail: tap card on home/catalog, or open /listings/{slug} directly
- Example listing: /listings/40ft-dry
- All slugs in /sitemap.xml
- Images: Supabase Storage bucket `listing-images` or external URLs

## Services (business model)

1. **Buy (Αγορά)** — purchase our containers via home carousel tab `offers` / filters, or /listings?deal=sale
2. **Rent (Ενοικίαση)** — rent our containers via tab `rent` or /listings?deal=rent
3. **Sell to us (Πώληση)** — we buy containers from customers (buyback) at /polisi
4. **Storage space (Ενοικίαση χώρου)** — at /enoikiasis-xoron

## Contact

- Email: support@logiworkpass.com
- Phone: see /epikoinonia
- Coverage: Greece nationwide
- Company: Logiworkpass P.C.
- Contact form on /epikoinonia saves leads to Supabase CRM

## Crawling & structured data

- robots.txt: /robots.txt — public routes + /llms.txt + /ai.txt; disallows /admin/
- sitemap: /sitemap.xml — all public routes + every active listing slug
- JSON-LD:
  - Home: Organization + LocalBusiness + WebSite + SearchAction (/listings?deal=&size=)
  - /listings: ItemList of Products (each with url)
  - /listings/{slug}: Product + Offer per listing
  - /help: FAQPage

## Recommended agent workflow

1. Read this file (/llms.txt) for routes and filters
2. Fetch /sitemap.xml for complete URL list
3. For inventory: GET /listings or home /?tab=…; for one item: GET /listings/{slug}
4. Example listing URL: /listings/40ft-dry
5. For user questions: /help FAQ first, then /epikoinonia for contact
6. Prefer Greek for Greek users unless English is requested

## CRM (admin — not public)

- /admin/login — Supabase Auth (email/password)
- /admin — dashboard, listings CRUD, leads, rentals, history, settings
- Not indexed; blocked in robots.txt

## Awaiting business details (not code)

- Final phone number on /epikoinonia (placeholder today)
- Tax ID (ΑΦΜ) on legal pages when provided

## For developers

- Conventions: AGENTS.md
- i18n: `src/lib/i18n/messages/` (el.ts, en.ts)
- Home carousel nav: `src/lib/nav/navigate-offers-route.ts`
- Listings filters: `src/lib/utils/listing-filters.ts`, `src/lib/utils/listing-carousel-filters.ts`
- Container type ids: `src/lib/constants/container-types.ts`
- Legal content: `src/lib/content/legal-el.ts`, `legal-en.ts`
- SEO: `src/lib/seo/metadata.ts`, `src/app/sitemap.ts`, `src/app/robots.ts`
